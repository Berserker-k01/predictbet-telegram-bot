services:
  bot:
    build: .
    container_name: predictbet-bot-en
    restart: unless-stopped
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8789:8789"
    volumes:
      - ./data:/app/data
    environment:
      ADMIN_PORT: "8789"
      TZ: Africa/Lagos
      SESSION_FILE: "./data/sessions.json"
      ADMIN_STORE_FILE: "./data/admin.json"
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:8789/health').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
